spring:
  application:
    name: tf2026java-springmvc

  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: Hugo099!
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create