spring:
  application:
    name: intro_spring

  datasource:
    url: jdbc:postgresql://localhost:5432/intro_spring
    username: postgres
    password: Hugo099!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always

server:
  port: 8080