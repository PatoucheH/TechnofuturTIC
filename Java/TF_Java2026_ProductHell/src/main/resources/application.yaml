spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_bya
    username: postgres
    password: Hugo099!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000